[package]
name = "mcu-crypto"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Assembly-optimised, constant-time cryptography for 32-bit microcontrollers (Cortex-M, Xtensa). Currently P-256 and P-384."
repository = "https://github.com/kokroo/mcu-crypto"
categories = ["cryptography", "no-std", "embedded"]
keywords = ["crypto", "embedded", "no-std", "cortex-m", "p256"]

[dependencies]

[dev-dependencies]
num-bigint = "0.4"
num-traits = "0.2"
hex = "0.4"
proptest = "1"

[features]
default = []
# Force the portable backend even on an arch that has assembly. Used by the
# differential tests: they need BOTH backends live in one binary to compare.
force-portable = []

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = true

[profile.bench]
opt-level = 3
lto = true
codegen-units = 1
debug = true
